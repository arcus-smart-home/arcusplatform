////////////////////////////////////////////////////////////////////////////////
// This file has been generated by the Iris Protocol Compiler. To change
// this file please change the underlying IRP file.
////////////////////////////////////////////////////////////////////////////////
package {{package}};

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;
import org.junit.runners.Suite;

import java.io.DataOutputStream;
import java.io.DataInputStream;

import java.io.ByteArrayOutputStream;
import java.io.ByteArrayInputStream;

import java.nio.ByteBuffer;

import io.netty.buffer.ByteBuf;
import io.netty.buffer.Unpooled;

import static org.junit.Assert.*;

////////////////////////////////////////////////////////////////////////////////
// The class {{className}} is an immutable object and the implementation
// of this class enforces that to the extent that Java allows. 
//
// The main exception to this enforcement are arrays. Arrays are declared final
// but the contents of the array can still be changed in Java. THIS SHOULD
// NOT BE DONE. Arrays should be treated as if their contents are immutable.
//
// To create objects of type {{className}}:
//
// To clone and mutate objects of type {{className}}:
////////////////////////////////////////////////////////////////////////////////

public final class Test{{classname}} {
   {{#structs}}
   @RunWith(JUnit4.class)
   public static final class Test{{simpleclassname}} {
      {{>StructTestBody}}
   }
   {{/structs}}
}
